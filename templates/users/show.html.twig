{% extends 'base.html.twig' %}

{% block title %}User show{% endblock %}

{% block body %}
<div class="user_show">
    <div class="profil_img">
        <img src="{{ asset( user.thumbnail ) }}" alt="photo"/>
    </div>
    <section class="profil_content">
        <div class="username">
            <h3>{{ user.username }}</h3> 
            <a href="{{ path('users_edit', {'id': user.id}) }}"><button>Modifier profil</button></a>
        </div>
        <ul class="count">
            <li>Nb publications</li>
            <li>Nb abonnés</li>
            <li>Nb abonnements</li>
        </ul>
        <div class="description">
            <p>{{ user.name }}</p>
        <p>{{ user.description }} </p> 
        </div>
    </section>
</div>


<a href="{{ path('users_index') }}">back to list</a>
{{ include('users/_delete_form.html.twig') }}   
{% endblock %}


