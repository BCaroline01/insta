{% extends 'base.html.twig' %}

{% block title %}Hashtags{% endblock %}

{% block body %}
    <h1>Hashtags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hashtag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hashtag.Name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ hashtag.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hashtags_index') }}">back to list</a>

    <a href="{{ path('hashtags_edit', {'id': hashtag.id}) }}">edit</a>

    {{ include('hashtags/_delete_form.html.twig') }}
{% endblock %}
